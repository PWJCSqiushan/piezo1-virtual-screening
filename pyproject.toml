[project]
name = "piezo1-vs"
version = "0.2.0"
description = "Reproducible same-structure multi-tool pocket consensus for PIEZO1 virtual screening"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
